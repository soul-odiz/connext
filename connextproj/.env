# ============ LOCAL DEVELOPMENT ============
# These values are safe for local dev only.
# In production, set these via CI/CD secrets or Azure Container App environment variables.

REACT_APP_API_BASE_URL=http://localhost:5000